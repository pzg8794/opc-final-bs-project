# Import Notes

Rclone exit code: 0

Filters: source/code/docs/images under 25 MiB were copied; large binaries/media/archives/installers were left in Drive.

```
2026/05/31 02:34:40 ERROR : Using --filter is recommended instead of both --include and --exclude as the order they are parsed in is indeterminate

```
